# Lambda functions are  functions you create to be used instantly
# They are anonymous functions
# It is a one line function

# func = lambda x: x ** 2
# print(func(5))

func = lambda x, y: (x ** 2) + y
print(func(5, 7))
